# 08：慢加载和绘制故障仍能进入有意义的画面

状态：待拆分确认，尚未发布。发布后为 ready-for-agent；有阻塞边时不可提前领取。

## Parent

本轮《Mira 全范围打磨》Spec，发布后填写真实链接。

## What to build

完善素材/首帧就绪、冷启动、缺图、三维不可用与context恢复路径，让完整开场不先于画面发生，失败后可重试。

## Acceptance criteria

- [ ] 主素材或有效程序化回退可呈现后才开始完整开场；慢网/404不导致无限等待。
- [ ] WebGL不可用/context lost有清楚的静态后备和重试；恢复后可探索，交互不重复注册。
- [ ] 回访不增加强制等待仪式；加载静帧与实时画面衔接明确，不假装后备可探索。
- [ ] 加载静帧与静态后备记录来源场景版本；01/02 新美术落地后由 11 重制核对，不以旧美术画面冒充新场景。
- [ ] 记录冷启动/解析测量，必要时异步加载场景；不把拆包数量当加载改善证据。
- [ ] 故障注入验证非空画面与恢复操作，行为和视觉回退各留证。

## Blocked by

None (can start immediately).

