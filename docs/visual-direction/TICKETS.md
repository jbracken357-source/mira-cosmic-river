# 下一轮视觉升级任务草案

2026-09-12。尚未发布到 GitHub。关联既有 #1；不是重新开启已关闭的 #4。执行前核对最新主分支、合并状态和新票，防止重复。

## V1：把局部气流素材接入正式光河

2026-09-13 状态：本地已实现，构建/lint、42 项回归通过；随后窄屏 FOV 修正相关 9 项重跑通过。人工检查默认、多个旋转视角、手机、素材失败回退；尚未部署，整体美术未验收。见 INTEGRATION.md。

依赖：当前主项目合并完成；重读最新 Scene/MiraTail 与移动端质量设置。

以 experiments/river-study 的密度纹理和少量弯曲层为起点，复用现有粒子、入场透明度与质量档。实验渲染器不搬入正式应用，正式实现继续使用 R3F。保留原有探索、点击尾巴与 reduced-motion 契约。验证原来是否还有材质 ref 问题，只在复现后修复。

验收：默认及边界视角无矩形边缘/明显薄片；漂移、视差可见；加载失败仍有既有粒子或程序化回退；资源释放明确；移动低档减少层数；构建、lint、相关 E2E 通过，附同视角 before/after。若无法支撑现有自由视角，先解决几何方案，不直接限制原交互。

## V2：双星的温度与牵引

2026-09-13 状态：本地首版已实现。新增球面密度图、程序化接缝/缺图回退、动态明暗；吸积盘降低过曝并加入剪切纹理，物质流修正端点与旋转脱节；公转放慢，移除常驻示意线。美术最终验收待观看者确认。

依赖：V1，以接入后的光河作为曝光与尺度上下文。

红巨星增加柔和大气与细节层次，小星以柔软不对称吸积光流呈现；物质流连接两者。避免球面贴图烘焙死的高光、土星式硬环、发白过曝。复用现有 shader，不引入新渲染引擎。

验收：近看有体积，远看双星均可辨；暖金与蓝白对照清晰；缓慢运动表达相伴；当前时钟驱动与信息卡不回归；桌面近/远及手机截图获观看者认可。

## V3：从相伴到无尽光河的镜头

2026-09-13 状态：本地首版已实现。远景包含主要光河；竖屏斜向构图与相机预设；结语镜头拉远，修复完整开场交给 OrbitControls 时的目标不一致。既有直达/重播保留。

依赖：V1、V2。

完整开场由亲密近景拉到巨大空间，光河逐层显露；回访直达继续沿用当前实现。为竖屏独立设计同一场景构图。说明保持轻量，减少动态效果时跳过戏剧性镜头。

验收：双星不被裁出竖屏；近远之间有真实视差；没有空黑等待或相机突跳；直达、重播、触控、reduced-motion 相关 E2E 通过，附镜头前/中/后三帧。

## V4：材质成本与视觉收口

2026-09-13 状态：两张 WebP 共 661906 bytes，比 PNG 原始 4248173 bytes 减少约 84.4%；桌面浏览器与模拟竖屏检查已做。真实手机帧率/发热和观看者最终美术验收尚未完成，不能关闭该验收项。

依赖：V1—V3。

根据实际素材用途压缩与分辨率分档，在选定真机检查流畅度、发热与触控。将已确认变化补入 CONTEXT/ADR 与 #1，纠正“真实轨道”等不再合适的承诺，同时尊重已完成实现。

验收：真实设备信息及测量方法齐全；目标交互约 30 fps，失败有降档验证；记录素材来源/参数/使用位置；观看者确认美、浪漫与震撼；当前 CI 通过。发布与部署状态单独记录。
